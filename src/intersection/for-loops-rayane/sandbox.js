import { findIntersection } from './intersection-rayane.js';

/* const findIntersection = (array = [], values = []) => {
*
  //initialize array to contain common items
  const commonItems = [];
*
  //using nested loops on length of both arrays
  for (let i = 0; i < array.length; i++) {
    for (let j = 0; j < value.length; j++) {
      if (array[i] === value[j]) {
        // if item is present in both arrays
        commonItems.push(array[i]); // add to the new array
      }
    }
  }
*
  return commonItems; // return the common items
}; */
